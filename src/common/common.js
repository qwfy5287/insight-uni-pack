/**
 * 处理异常，输出信息，不中断代码
 * @param {Object} err 异常信息
 */
export const handleCatch = err => {
  console.error('🚀 ~ file: common.js ~ line 6 ~ handleCatch ~ err', err)
}
